import { ReactNode } from "react";
export type SectionProps = {
    width?: string;
    children?: ReactNode;
};
export declare const Section: ({ children, width }: SectionProps) => import("react/jsx-runtime").JSX.Element;
